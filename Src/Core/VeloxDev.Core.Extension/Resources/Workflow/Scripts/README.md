# Workflow Scripts

Place language-neutral instruction scripts in this directory.
Scripts are embedded without a language suffix and injected verbatim into the system prompt under `## 🔧 Scripts`.

File naming: `{Name}.md` or any extension — no language code in the filename.
