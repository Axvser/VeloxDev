# DynamicTheme 测试

针对 `VeloxDev.DynamicTheme` 命名空间的单元测试 —— 主题系统基础类型与管理器。

| 测试文件 | 测试目标 | 关键场景 |
|---|---|---|
| `ThemeBasicsTests.cs` | `Dark`、`Light`、`ThemeManager`、`StartModel`、`ThemeConfigAttribute` | ITheme 实现验证、默认/切换当前主题、StartModel 标志枚举、泛型特性实例化 |
