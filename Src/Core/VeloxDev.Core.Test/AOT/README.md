# AOT 测试

针对 `VeloxDev.AOT` 命名空间的单元测试 —— AOT 反射保留特性。

| 测试文件 | 测试目标 | 关键场景 |
|---|---|---|
| `AOTReflectionAttributeTests.cs` | `AOTReflectionAttribute` | 默认参数值、自定义参数、AttributeUsage 约束（Class/Struct、不可重复、不可继承） |
