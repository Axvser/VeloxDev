<?xml version="1.0" encoding="utf-8" ?>
<!-- VeloxDev customization: Generate the Node, Slot, Link, selector, and grid-decorator templates, then update the local type names below if you renamed them. -->
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TemplateNamespace"
             xmlns:behaviors="clr-namespace:VeloxDev.WorkflowSystem.AttachedBehaviors;assembly=VeloxDev.MAUI"
             x:Class="TemplateNamespace.TemplateClass"
             behaviors:WorkflowSurfaceBehavior.IsEnabled="True" behaviors:WorkflowSurfaceBehavior.ScrollViewerName="PART_ScrollViewer" behaviors:WorkflowSurfaceBehavior.CanvasName="PART_Canvas" behaviors:WorkflowSurfaceBehavior.GridDecoratorName="PART_GridDecorator" behaviors:WorkflowSurfaceBehavior.PointerPressSourceName="PART_SurfaceBorder">
    <ContentView.Resources>
        <DataTemplate x:Key="NodeTemplate">
            <local:NodeView WidthRequest="{Binding Size.Width}" HeightRequest="{Binding Size.Height}" />
        </DataTemplate>
        <DataTemplate x:Key="LinkTemplate">
            <local:LinkView StartLeft="{Binding Sender.Anchor.Horizontal}" StartTop="{Binding Sender.Anchor.Vertical}" EndLeft="{Binding Receiver.Anchor.Horizontal}" EndTop="{Binding Receiver.Anchor.Vertical}" CanRender="{Binding IsVisible}" />
        </DataTemplate>
        <local:TemplateSelector x:Key="WorkflowTemplateSelector" NodeTemplate="{StaticResource NodeTemplate}" LinkTemplate="{StaticResource LinkTemplate}" />
    </ContentView.Resources>

    <Border x:Name="PART_SurfaceBorder"
            Stroke="TemplateSurfaceBorderBrush"
            StrokeThickness="TemplateSurfaceBorderThickness"
            StrokeShape="RoundRectangle TemplateSurfaceCornerRadius"
            BackgroundColor="TemplateSurfaceBackground">
        <local:GridDecorator x:Name="PART_GridDecorator">
            <Grid RowDefinitions="28,*" ColumnDefinitions="28,*">
                <ScrollView x:Name="PART_ScrollViewer"
                            Grid.Row="1"
                            Grid.Column="1"
                            Orientation="Both">
                    <AbsoluteLayout x:Name="PART_Canvas"
                                    BackgroundColor="Transparent"
                                    behaviors:ViewPool.ItemsSource="{Binding Helper.VisibleItems}"
                                    behaviors:ViewPool.TemplateSelector="{StaticResource WorkflowTemplateSelector}" />
                </ScrollView>
            </Grid>
        </local:GridDecorator>
    </Border>
</ContentView>
