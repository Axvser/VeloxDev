# 动画

几乎任何GUI框架对于动画的处理都有专有接口，这使得插值动画的创建在一定程度上依赖GUI的语法设计，并且可能存在限制，以 WPF 为例：标准的动画必须基于 DependencyProperty 构建，虽然是为了利于内部性能优化，但同时也损失了自由度

VeloxDev 倡导兼容性，内部设计了动画从构建到执行的完整抽象层，每个GUI框架只需少量适配代码，最终动画作用于标准属性而非任何GUI特有对象

> **Fluent - 优雅地构建动画**

> **快照 - 状态记录与恢复**

> **主题 - 生动的主题配色切换实现**