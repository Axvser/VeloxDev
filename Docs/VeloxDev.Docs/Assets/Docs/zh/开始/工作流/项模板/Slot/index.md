# Slot

> **创建 - {$plateform} 可选 ava / wpf / winui / maui**

```shellscript
dotnet new {$plateform}-v-slot -n SlotView -ns MyApp.Views
```

> **个性参数**

| 选项 | 含义 |
| --- | --- |
| -bg | 背景色 |
| -sc | 待机色 |
| -bc | 边框色 |
| -sp | SVG路径 |