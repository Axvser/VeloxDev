# Tree 视图模板

生成绑定到 `IWorkflowTreeViewModel` 的 Tree 视图。

```shell
# Avalonia
dotnet new ava-v-tree -n WorkflowTreeView -ns MyApp.Views

# WPF
dotnet new wpf-v-tree -n WorkflowTreeView -ns MyApp.Views
```

生成的视图包含可滚动画布表面。
