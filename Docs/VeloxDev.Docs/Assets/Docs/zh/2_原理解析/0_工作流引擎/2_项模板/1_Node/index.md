# Node 视图模板

生成绑定到 `IWorkflowNodeViewModel` 的 Node 视图。

```shell
# Avalonia
dotnet new ava-v-node -n MyNodeView -ns MyApp.Views

# WPF
dotnet new wpf-v-node -n MyNodeView -ns MyApp.Views
```

生成的视图包含节点头部、插槽区和主体区域。
