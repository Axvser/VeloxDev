# Link 视图模板

生成绑定到 `IWorkflowLinkViewModel` 的 Link 视图。

```shell
# Avalonia
dotnet new ava-v-link -n MyLinkView -ns MyApp.Views

# WPF
dotnet new wpf-v-link -n MyLinkView -ns MyApp.Views
```

生成的视图将连接渲染为贝塞尔曲线或折线。
