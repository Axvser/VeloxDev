# Template Selector

> **Create - {$plateform} optional ava / wpf / winui / maui, where ava can skip using this template.**

```shellscript
dotnet new {$plateform}-v-selector -n ComponentSelector -ns MyApp.Views
```