# Solution

Check your component mounting order. The Node component must be mounted to the Tree component before internal commands can be executed. The Slot component must be mounted to the Node component before internal commands can be executed.