namespace Demo.ViewModels;

public partial class MainWindowViewModel : ViewModelBase
{
    public string Greeting { get; } = "Keep the window focused | The hotkey [ Ctrl + H ] can be checked every frame";
}