<UserControl x:Class="Demo.Views.NodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Demo.Views"
             xmlns:vm="clr-namespace:Demo.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="12*" />
            <RowDefinition Height="90*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="1" 
                Padding="1"
                Background="#33ffffff" 
                BorderBrush="White"
                BorderThickness="1,0,1,1" 
                CornerRadius="0,0,4,4" />

        <!-- 标题栏 -->
        <Border Grid.Row="0"
                Background="#aaffffff"
                MouseDown="OnMouseDown"
                MouseUp="OnMouseUp"
                MouseMove="OnMouseMove"
                MouseLeave="OnMouseLeave">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock  Text="执行数 [ " VerticalAlignment="Center"/>
                <TextBlock  Text="{Binding RunCount}" VerticalAlignment="Center"/>
                <TextBlock  Text=" ] " VerticalAlignment="Center"/>
                <TextBlock  Text="缓冲数 [ " VerticalAlignment="Center"/>
                <TextBlock  Text="{Binding WaitCount}" VerticalAlignment="Center"/>
                <TextBlock  Text=" ] " VerticalAlignment="Center"/>
            </StackPanel>
            
        </Border>

        <!-- 插槽区域 -->
        <Grid Grid.Row="0" Grid.RowSpan="2">
            <ItemsControl ItemsSource="{Binding Slots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}"
                                Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Grid}}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding Offset.Left}" />
                        <Setter Property="Canvas.Top" Value="{Binding Offset.Top}" />
                        <Setter Property="Panel.ZIndex" Value="{Binding Offset.Layer}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:SlotViewModel}">
                        <local:SlotView Width="{Binding Size.Width}"
                                        Height="{Binding Size.Height}"
                                        IsEnabled="{Binding IsEnabled}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>