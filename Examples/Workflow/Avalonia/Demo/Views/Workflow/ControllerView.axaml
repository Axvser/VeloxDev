<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="450"
			 x:Class="Demo.ControllerView"
			 xmlns:local="using:Demo"
            xmlns:behaviors="using:VeloxDev.WorkflowSystem.AttachedBehaviors"
			 xmlns:vm="using:Demo.ViewModels"
             ClipToBounds="False"
           behaviors:WorkflowSlotLayoutBehavior.IsEnabled="True"
			 behaviors:WorkflowSlotLayoutBehavior.SlotNames="PART_OutputSlot"
			 behaviors:WorkflowSlotLayoutBehavior.CoordinateHostName="PART_Canvas"
			 x:DataType="vm:ControllerViewModel">
  <Grid RowDefinitions="52,*,88" ColumnDefinitions="*" ClipToBounds="False">
		<Border Grid.RowSpan="5"
             Grid.ColumnSpan="2"
				BorderThickness="1"
				CornerRadius="5"
				BorderBrush="White"
				Background="#2d2d2d" />
		<Border Grid.Row="0"
				Grid.ColumnSpan="2"
				BorderThickness="1"
				CornerRadius="5,5,0,0"
				Background="#2d2d2d"
                behaviors:WorkflowNodeDragBehavior.CoordinateHostName="PART_Canvas"
				behaviors:WorkflowNodeDragBehavior.IsEnabled="True">
         <TextBlock Text="Network Flow Controller"
					   VerticalAlignment="Center"
					   HorizontalAlignment="Center"
					   Foreground="White"/>
		</Border>

        <Border Grid.Row="1" Background="#252525" Padding="12">
			<ScrollViewer>
			<StackPanel Spacing="6">
               <TextBlock Text="Seed" Foreground="#dcdcdc" FontSize="11"/>
				<TextBox Text="{Binding SeedPayload, Mode=TwoWay}" Watermark="payload" FontSize="11"/>
			</StackPanel>
			</ScrollViewer>
		</Border>

		<Border Grid.Row="2" Background="#2d2d2d" CornerRadius="0,0,5,5" Padding="8">
			<Grid ColumnDefinitions="*,*" RowDefinitions="*,*" ColumnSpacing="6" RowSpacing="6">
				<Button Grid.Row="0" Grid.Column="0"
						Content="Compile"
						VerticalContentAlignment="Center"
						Background="#1e3a5f"
						Foreground="White"
						Command="{Binding CompileCommand}"/>
				<Button Grid.Row="0" Grid.Column="1"
						Content="Run"
						VerticalContentAlignment="Center"
						Background="#1e5f2e"
						Foreground="White"
						IsEnabled="{Binding HasCompiledGraphs}"
						Command="{Binding RunCommand}"/>
				<Button Grid.Row="1" Grid.Column="0"
						Content="Stop"
						VerticalContentAlignment="Center"
						Background="#5f1e1e"
						Foreground="White"
						Command="{Binding StopCommand}"/>
				<Button Grid.Row="1" Grid.Column="1"
						Content="Close"
						VerticalContentAlignment="Center"
						Background="#1e1e1e"
						Foreground="White"
						Command="{Binding CloseWorkflowCommand}"/>
			</Grid>
		</Border>

     <local:SlotView x:Name="PART_OutputSlot"
					 Width="20"
					 Height="20"
					 Grid.RowSpan="3"
					 HorizontalAlignment="Right"
					 VerticalAlignment="Center"
					 Margin="0,0,-10,0"
					 DataContext="{Binding OutputSlot}"
					 SlotState="{Binding State}"/>
	</Grid>
</UserControl>
