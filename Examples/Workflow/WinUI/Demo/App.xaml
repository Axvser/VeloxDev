<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Demo.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- 节点卡共用的一套（卡面、字级、幽灵按钮、输入框各状态）。五张卡都从它取
                     Style="{StaticResource CardXxx}"，不各自写色值。位置与 Avalonia 把
                     StyleInclude 挂在 Application.Styles 上一样。

                     注意顺序：它排在 XamlControlsResources 之后，所以里面 TextControl* /
                     ComboBox* 那几个键盖掉的是 Fluent 默认值（轻量级样式，微软文档的
                     "lightweight styling"），而不是被默认值盖掉。 -->
                <ResourceDictionary Source="ms-appx:///Views/Workflow/CardTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>
