<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Demo.Views.ControllerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52" />
            <RowDefinition Height="*" />
            <RowDefinition Height="56" />
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="3"
                BorderThickness="1"
                CornerRadius="5"
                BorderBrush="White"
                Background="#2D2D2D" />

        <Border Grid.Row="0"
                CornerRadius="5,5,0,0"
                Background="#2D2D2D"
                PointerPressed="OnPointerPressed"
                PointerReleased="OnPointerReleased"
                PointerMoved="OnPointerMoved"
                PointerCaptureLost="OnPointerCaptureLost">
            <TextBlock Text="Network Flow Controller"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="White" />
        </Border>

        <Border Grid.Row="1" Background="#252525" Padding="16">
            <StackPanel Spacing="10">
                <TextBlock Text="种子负载" Foreground="#DCDCDC" />
                <TextBox Text="{Binding SeedPayload, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="控制器节点只负责把初始上下文送入工作流。后续节点是否继续广播，仅由节点自身决定。"
                           Foreground="#BDBDBD"
                           TextWrapping="WrapWholeWords" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#2D2D2D" CornerRadius="0,0,5,5" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Margin="0,0,4,0" Command="{Binding OpenWorkflowCommand}">
                    <TextBlock Text="Run Flow" Foreground="White" />
                </Button>
                <Button Grid.Column="1" Margin="4,0,0,0" Command="{Binding CloseWorkflowCommand}">
                    <TextBlock Text="Close Tree" Foreground="White" />
                </Button>
            </Grid>
        </Border>

        <local:SlotView x:Name="PART_OutputSlot"
                        Width="20"
                        Height="20"
                        Grid.RowSpan="3"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,-10,0"
                        DataContext="{Binding OutputSlot}"
                        SlotState="{Binding State}" />
    </Grid>
</UserControl>
