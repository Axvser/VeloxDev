<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Demo.Views.TreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Demo.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:TreeViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="NodeTemplate">
            <local:NodeView Width="{Binding Size.Width}"
                            Height="{Binding Size.Height}"
                            Canvas.Left="{Binding Anchor.Left}"
                            Canvas.Top="{Binding Anchor.Top}"
                            Canvas.ZIndex="{Binding Anchor.Layer}"/>
        </DataTemplate>
        <DataTemplate x:Key="ControllerTemplate">
            <local:ControllerView Width="{Binding Size.Width}"
                                  Height="{Binding Size.Height}"
                                  Canvas.Left="{Binding Anchor.Left}"
                                  Canvas.Top="{Binding Anchor.Top}"
                                  Canvas.ZIndex="{Binding Anchor.Layer}"/>
        </DataTemplate>
        <local:CustomTemplateSelector x:Key="NodeSelector"
                                      ControllerTemplate="{StaticResource ControllerTemplate}"
                                      NodeTemplate="{StaticResource NodeTemplate}"/>
    </UserControl.Resources>

    <Grid Background="#020617">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Margin="20"
                Padding="18"
                Background="#111827"
                CornerRadius="18"
                BorderBrush="#334155"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Workflow Demo"
                               Foreground="White"
                               FontSize="26"
                               FontWeight="Bold" />
                    <TextBlock Margin="0,10,0,0"
                               Text="展示网络请求链路、广播执行顺序与节点状态变化。"
                               Foreground="#CBD5E1"
                               TextWrapping="WrapWholeWords" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,18,0,0" Spacing="10">
                    <Button Content="Undo" Height="42" Command="{Binding UndoCommand}" />
                    <Button Content="Redo" Height="42" Command="{Binding RedoCommand}" />
                    <Button Content="Save" Height="42" Click="SaveWorkflow" />
                    <Button Content="Load" Height="42" Click="SelectWorkflow" />
                    <Button Content="Reset Demo" Height="42" Click="SimulateData" />
                </StackPanel>

                <Border Grid.Row="2"
                        Margin="0,18,0,0"
                        Padding="12"
                        Background="#0F172A"
                        CornerRadius="12"
                        BorderBrush="#1E293B"
                        BorderThickness="1">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Execution Log"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   FontSize="16" />
                        <TextBlock Text="运行后会按顺序记录 Controller 与各节点执行轨迹。"
                                   Foreground="#94A3B8"
                                   FontSize="11"
                                   TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </Border>

                <ListView Grid.Row="3"
                          Margin="0,14,0,0"
                          ItemsSource="{Binding ExecutionLog}"
                          Background="#0B1120"
                          BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       Foreground="#E2E8F0"
                                       TextWrapping="WrapWholeWords" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Margin="0,20,20,20"
                Background="#0B1120"
                CornerRadius="18"
                BorderBrush="#1E293B"
                BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          ZoomMode="Disabled">
                <Canvas x:Name="canvas"
                        Width="1480"
                        Height="920"
                        Background="#0F172A"
                        PointerMoved="Canvas_PointerMoved"
                        PointerReleased="Canvas_PointerReleased">
                    <local:CanvasItemsControl ItemsSource="{Binding Nodes}"
                                              Canvas.ZIndex="1"
                                              ItemTemplateSelector="{StaticResource NodeSelector}">
                        <local:CanvasItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </local:CanvasItemsControl.ItemsPanel>
                    </local:CanvasItemsControl>

                    <ItemsControl ItemsSource="{Binding Links}" Canvas.ZIndex="-1">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:LinkView StartLeft="{Binding Sender.Anchor.Left}"
                                                StartTop="{Binding Sender.Anchor.Top}"
                                                EndLeft="{Binding Receiver.Anchor.Left}"
                                                EndTop="{Binding Receiver.Anchor.Top}"
                                                CanRender="{Binding IsVisible}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <local:LinkView DataContext="{Binding VirtualLink}"
                                    Canvas.ZIndex="-1"
                                    StartLeft="{Binding Sender.Anchor.Left}"
                                    StartTop="{Binding Sender.Anchor.Top}"
                                    EndLeft="{Binding Receiver.Anchor.Left}"
                                    EndTop="{Binding Receiver.Anchor.Top}"
                                    CanRender="{Binding IsVisible}"
                                    IsVirtual="True"/>
                </Canvas>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
