<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Demo.Views.NodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:VeloxDev.WorkflowSystem.AttachedBehaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Clip="{x:Null}"
    mc:Ignorable="d">

    <!-- 兜底节点卡：节点类型自带视图时不会用到。故意留空——IWorkflowNodeViewModel 只有几何与插槽、
         没有显示名；Chrome 属视图而非 ViewModel；WinUI 默认裁剪内容，故与同级 NodeView 一样清掉 Clip -->
    <Grid Clip="{x:Null}">
        <Border CornerRadius="6"
                Background="#1E293B"
                BorderBrush="#334155"
                BorderThickness="1.5"
                behaviors:WorkflowNodeDragBehavior.IsEnabled="True"
                behaviors:WorkflowNodeDragBehavior.CoordinateHostName="PART_Canvas" />
    </Grid>
</UserControl>
