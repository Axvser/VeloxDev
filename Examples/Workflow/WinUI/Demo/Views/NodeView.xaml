<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Demo.Views.NodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border Background="#0F172A"
                BorderBrush="#334155"
                BorderThickness="1"
                CornerRadius="12" />

        <Grid Margin="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="34" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#111827"
                    CornerRadius="11,11,0,0"
                    BorderBrush="#1F2937"
                    BorderThickness="0,0,0,1"
                    ManipulationDelta="Border_ManipulationDelta"
                    ManipulationMode="All">
                <Grid Margin="10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Title}"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontSize="13"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis" />
                    <Border Grid.Column="1"
                            Margin="8,0,0,0"
                            Padding="8,2"
                            Background="#1D4ED8"
                            CornerRadius="8"
                            VerticalAlignment="Center">
                        <TextBlock Text="{Binding ExecutionOrderText}"
                                   Foreground="White"
                                   FontSize="10"
                                   FontWeight="Bold" />
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="12,10,12,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Padding="8,2"
                            Background="#0B5ED7"
                            CornerRadius="8"
                            HorizontalAlignment="Left">
                        <TextBlock Text="{Binding Method}"
                                   Foreground="White"
                                   FontSize="10"
                                   FontWeight="Bold" />
                    </Border>
                    <TextBlock Grid.Column="1"
                               Text="{Binding LastStatus}"
                               Foreground="#93C5FD"
                               FontSize="11"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                </Grid>

                <TextBlock Grid.Row="1"
                           Margin="0,8,0,0"
                           Text="{Binding Url}"
                           Foreground="#E5E7EB"
                           FontSize="10"
                           TextWrapping="WrapWholeWords"
                           TextTrimming="CharacterEllipsis"
                           MaxLines="2" />

                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Foreground="#94A3B8"
                               FontSize="10">
                        <Run Text="Duration: " />
                        <Run Text="{Binding LastDuration}" />
                    </TextBlock>
                    <TextBlock Grid.Column="1"
                               Foreground="#94A3B8"
                               FontSize="10"
                               HorizontalAlignment="Right">
                        <Run Text="Run: " />
                        <Run Text="{Binding RunCount}" />
                    </TextBlock>
                </Grid>

                <TextBlock Grid.Row="3"
                           Margin="0,6,0,0"
                           Foreground="#94A3B8"
                           FontSize="10">
                    <Run Text="Queue: " />
                    <Run Text="{Binding WaitCount}" />
                </TextBlock>

                <StackPanel Grid.Row="4" Margin="0,8,0,0" Spacing="6">
                    <TextBlock Foreground="#CBD5E1"
                               FontSize="10"
                               TextWrapping="WrapWholeWords"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="2">
                        <Run Text="Trace: " />
                        <Run Text="{Binding LastExecutionTrace}" />
                    </TextBlock>
                    <TextBlock Foreground="#E2E8F0"
                               FontSize="10"
                               TextWrapping="WrapWholeWords"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="3">
                        <Run Text="Preview: " />
                        <Run Text="{Binding LastResponsePreview}" />
                    </TextBlock>
                    <TextBlock Foreground="#FCA5A5"
                               FontSize="10"
                               TextWrapping="WrapWholeWords"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="2">
                        <Run Text="Error: " />
                        <Run Text="{Binding LastError}" />
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Grid>

        <local:CanvasItemsControl ItemsSource="{Binding Slots}">
            <local:CanvasItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </local:CanvasItemsControl.ItemsPanel>
            <local:CanvasItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:SlotView Width="{Binding Size.Width}"
                                    Height="{Binding Size.Height}"
                                    Canvas.Left="{Binding Offset.Left}"
                                    Canvas.Top="{Binding Offset.Top}"
                                    Canvas.ZIndex="{Binding Anchor.Layer}"/>
                </DataTemplate>
            </local:CanvasItemsControl.ItemTemplate>
        </local:CanvasItemsControl>
    </Grid>
</UserControl>
