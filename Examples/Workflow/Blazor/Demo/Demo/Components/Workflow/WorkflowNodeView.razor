@using VeloxDev.WorkflowSystem
@using System.ComponentModel
@implements IDisposable

@* Generic catch-all body rendered inside the TemplateNodeView card when no dedicated
   body view exists for the node type. Title and execution feedback are shown by the
   surrounding card; this body adds no worker-specific inputs. *@
