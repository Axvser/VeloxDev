<UserControl x:Class="Demo.Views.Workflow.WorkflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Demo.Views.Workflow"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1280">
    <UserControl.Resources>
        <DataTemplate x:Key="NodeTemplate">
            <local:NodeView Width="{Binding Size.Width}"
                            Height="{Binding Size.Height}"
                            IsEnabled="{Binding IsEnabled}"/>
        </DataTemplate>
        <DataTemplate x:Key="ControllerTemplate">
            <local:ControllerView Width="{Binding Size.Width}"
                                  Height="{Binding Size.Height}"
                                  IsEnabled="{Binding IsEnabled}"/>
        </DataTemplate>
        <local:CustomTemplateSelector x:Key="NodeSelector"
                                      Controller="{StaticResource ControllerTemplate}"
                                      Simulator="{StaticResource NodeTemplate}"/>
    </UserControl.Resources>

    <Grid Background="#020617">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Margin="20"
                Padding="18"
                Background="#111827"
                CornerRadius="18"
                BorderBrush="#334155"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Workflow Demo"
                               Foreground="White"
                               FontSize="26"
                               FontWeight="Bold" />
                    <TextBlock Margin="0,10,0,0"
                               Text="展示网络请求链路、广播执行顺序与节点状态变化。"
                               Foreground="#CBD5E1"
                               TextWrapping="Wrap" />
                </StackPanel>

                <UniformGrid Grid.Row="1"
                             Margin="0,18,0,0"
                             Columns="1">
                    <Button Content="Undo" Height="42" Margin="0,0,0,10" Command="{Binding UndoCommand}" />
                    <Button Content="Redo" Height="42" Margin="0,0,0,10" Command="{Binding RedoCommand}" />
                    <Button Content="Save" Height="42" Margin="0,0,0,10" Click="SaveWorkflow" />
                    <Button Content="Load" Height="42" Margin="0,0,0,10" Click="SelectWorkflow" />
                    <Button Content="Reset Demo" Height="42" Click="SimulateData" />
                </UniformGrid>

                <Border Grid.Row="2"
                        Margin="0,18,0,0"
                        Padding="12"
                        Background="#0F172A"
                        CornerRadius="12"
                        BorderBrush="#1E293B"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Execution Log"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   FontSize="16" />
                        <TextBlock Margin="0,6,0,0"
                                   Text="运行后会按顺序记录 Controller 与各节点执行轨迹。"
                                   Foreground="#94A3B8"
                                   FontSize="11"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <ListBox Grid.Row="3"
                         Margin="0,14,0,0"
                         ItemsSource="{Binding ExecutionLog}"
                         Background="#0B1120"
                         BorderThickness="0"
                         Foreground="#E2E8F0"
                         ScrollViewer.VerticalScrollBarVisibility="Auto" />
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Margin="0,20,20,20"
                Background="#0B1120"
                CornerRadius="18"
                BorderBrush="#1E293B"
                BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          PanningMode="Both">
                <Canvas x:Name="WorkflowCanvas"
                        Width="1480"
                        Height="920"
                        Background="#0F172A"
                        MouseUp="OnPointerReleased"
                        MouseMove="OnPointerMoved">
                    <ItemsControl ItemsSource="{Binding Nodes}"
                                  Canvas.ZIndex="1"
                                  ItemTemplateSelector="{StaticResource NodeSelector}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding Anchor.Left}" />
                                <Setter Property="Canvas.Top" Value="{Binding Anchor.Top}" />
                                <Setter Property="Canvas.ZIndex" Value="{Binding Anchor.Layer}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding Links}" Canvas.ZIndex="-1">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:BezierCurveView StartLeft="{Binding Sender.Anchor.Left}"
                                                       StartTop="{Binding Sender.Anchor.Top}"
                                                       EndLeft="{Binding Receiver.Anchor.Left}"
                                                       EndTop="{Binding Receiver.Anchor.Top}"
                                                       CanRender="{Binding IsVisible}"
                                                       IsFlowEnabled="True"
                                                       FlowColor="#22D3EE"
                                                       FlowSpeed="3.0"
                                                       FlowDashArray="10,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <local:BezierCurveView DataContext="{Binding VirtualLink}"
                                           StartLeft="{Binding Sender.Anchor.Left}"
                                           StartTop="{Binding Sender.Anchor.Top}"
                                           EndLeft="{Binding Receiver.Anchor.Left}"
                                           EndTop="{Binding Receiver.Anchor.Top}"
                                           CanRender="{Binding IsVisible}"
                                           IsVirtual="True"/>
                </Canvas>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>