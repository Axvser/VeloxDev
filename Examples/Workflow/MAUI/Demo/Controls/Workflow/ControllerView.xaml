<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Demo.ViewModels;assembly=Lib"
             xmlns:controls="clr-namespace:Demo.Controls"
             xmlns:behaviors="clr-namespace:VeloxDev.WorkflowSystem.AttachedBehaviors;assembly=VeloxDev.MAUI"
             x:Class="Demo.Controls.ControllerView"
             behaviors:WorkflowSlotLayoutBehavior.IsEnabled="True"
             behaviors:WorkflowSlotLayoutBehavior.SlotNames="PART_OutputSlot"
             behaviors:WorkflowSlotLayoutBehavior.CoordinateHostName="PART_Canvas">
    <Grid RowDefinitions="52,*,56">
        <Border Grid.RowSpan="3"
                Stroke="White"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 5"
                BackgroundColor="#2D2D2D" />

        <Border Grid.Row="0"
                BackgroundColor="#2D2D2D"
                StrokeShape="RoundRectangle 5,5,0,0"
                behaviors:WorkflowNodeDragBehavior.CoordinateHostName="PART_Canvas"
                behaviors:WorkflowNodeDragBehavior.IsEnabled="True">
            <Label Text="Network Flow Controller"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   TextColor="White" />
        </Border>

        <Border Grid.Row="1" BackgroundColor="#252525" Padding="12">
            <ScrollView>
            <VerticalStackLayout Spacing="4">
                <Label Text="Seed" TextColor="#DCDCDC" FontSize="11" />
                <Entry Text="{Binding SeedPayload}" FontSize="11" />

                <Label Text="Mode" TextColor="#DCDCDC" FontSize="11" />
                <Picker ItemsSource="{Binding CompileModeOptions}"
                        SelectedItem="{Binding CompileMode}" FontSize="11" />

                <Label Text="Direction" TextColor="#DCDCDC" FontSize="11" />
                <Picker ItemsSource="{Binding CompileDirectionOptions}"
                        SelectedItem="{Binding CompileDirection}" FontSize="11" />

                <Label Text="Scope" TextColor="#DCDCDC" FontSize="11" />
                <Picker ItemsSource="{Binding CompileScopeOptions}"
                        SelectedItem="{Binding CompileScope}" FontSize="11" />

                <Label Text="Cycle" TextColor="#DCDCDC" FontSize="11" />
                <Picker ItemsSource="{Binding CycleHandlingOptions}"
                        SelectedItem="{Binding CycleHandling}" FontSize="11" />
            </VerticalStackLayout>
            </ScrollView>
        </Border>

        <Border Grid.Row="2" BackgroundColor="#2D2D2D" StrokeShape="RoundRectangle 0,0,5,5" Padding="8">
            <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                <Button Grid.Column="0" Text="Run Flow" Command="{Binding OpenWorkflowCommand}" />
                <Button Grid.Column="1" Text="Close Tree" Command="{Binding CloseWorkflowCommand}" />
            </Grid>
        </Border>

        <controls:SlotView x:Name="PART_OutputSlot"
                           WidthRequest="20"
                           HeightRequest="20"
                           Grid.RowSpan="3"
                           HorizontalOptions="End"
                           VerticalOptions="Center"
                           Margin="0,0,-10,0"
                           BindingContext="{Binding OutputSlot}"
                           SlotState="{Binding State}" />
    </Grid>
</ContentView>
