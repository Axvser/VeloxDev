<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Demo.Controls"
             x:Class="Demo.Controls.BoolSelectorNodeView"
             controls:WorkflowSlotLayoutBehavior.IsEnabled="True"
             controls:WorkflowSlotLayoutBehavior.SlotNames="PART_InputSlot"
             controls:WorkflowSlotLayoutBehavior.SlotEnumeratorNames="PART_OutputSlotsList"
             controls:WorkflowSlotLayoutBehavior.CoordinateHostName="PART_Canvas">
    <Grid RowDefinitions="48,*">
        <Border Grid.RowSpan="2"
                Stroke="#6EC6FF"
                StrokeThickness="1.5"
                StrokeShape="RoundRectangle 6"
                BackgroundColor="#1E2A35" />

        <Grid Grid.RowSpan="2" ZIndex="6" InputTransparent="True" CascadeInputTransparent="False">
            <controls:SlotView x:Name="PART_InputSlot"
                               WidthRequest="20"
                               HeightRequest="20"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Margin="-10,0,0,0"
                               BindingContext="{Binding InputSlot}"
                               IsVisible="{Binding HasInputSlot}"
                               SlotState="{Binding State}" />
        </Grid>

        <Border Grid.Row="0"
                BackgroundColor="#253545"
                Stroke="#6EC6FF"
                StrokeThickness="0"
                StrokeShape="RoundRectangle 4,4,0,0"
                controls:WorkflowNodeDragBehavior.CoordinateHostName="PART_Canvas"
                controls:WorkflowNodeDragBehavior.IsEnabled="True">
            <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="8">
                <Label Text="{Binding Title}" TextColor="White" FontAttributes="Bold" />
                <Border BackgroundColor="#1F3D1F" Stroke="#7BD97B" StrokeThickness="1" StrokeShape="RoundRectangle 10" Padding="6,1">
                    <Label Text="{Binding LastRouted}" TextColor="#C8FFC8" FontAttributes="Bold" />
                </Border>
            </HorizontalStackLayout>
        </Border>

        <Border Grid.Row="1" BackgroundColor="#1E2A35" StrokeShape="RoundRectangle 0,0,4,4">
            <VerticalStackLayout Margin="14" Spacing="10">
                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                    <CheckBox IsChecked="{Binding Condition}" />
                    <Label Grid.Column="1" Text="Condition = True" TextColor="#DCDCDC" VerticalTextAlignment="Center" />
                </Grid>
                <Label Text="Output Slots" TextColor="#BFBFBF" FontSize="11" Margin="0,4,0,0" />
                <VerticalStackLayout x:Name="PART_OutputSlotsList" BindableLayout.ItemsSource="{Binding OutputSlots.Items}" Spacing="4">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="*,Auto">
                                <Label Grid.Column="0"
                                       Text="{Binding Name}"
                                       TextColor="#6EC6FF"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="End"
                                       VerticalTextAlignment="Center"
                                       Margin="0,0,8,0" />
                                <controls:SlotView Grid.Column="1"
                                                   WidthRequest="16"
                                                   HeightRequest="16"
                                                   HorizontalOptions="End"
                                                   VerticalOptions="Center"
                                                   Margin="0,0,-8,0"
                                                   BindingContext="{Binding Slot}"
                                                   SlotState="{Binding State}" />
                            </Grid>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentView>
